package Dashboard;

import ComposantsInterfaces.Button;

import javax.swing.*;
import java.awt.*;

public class AdministrationPanel extends JPanel {

  /*  private JLabel welcomeLabel;
    private Button addBookButton;
    private Button removeBookButton;
    private Button editBookButton;
    private Button addUserButton;
    private Button removeUserButton;
    private Button editUserButton;

    private Button addExemplaireButton;
    private Button removeExemplaireButton;
    private Button editExemplaireButton;
    private Button manageBorrowingsButton;
    private Button statisticsButton;

   */

    public AdministrationPanel(String username) {
       /* welcomeLabel = new JLabel("Bienvenue, " + username + " !");
        addBookButton = new Button("Ajouter un livre");
        removeBookButton = new Button("Supprimer un livre");
        editBookButton = new Button("Modifier un livre");
        addUserButton = new Button("Ajouter un utilisateur");
        removeUserButton = new Button("Supprimer un utilisateur");
        editUserButton = new Button("Modifier un utilisateur");
        addExemplaireButton = new Button("Ajouter un exemplaire");
        editExemplaireButton = new Button("Modifier un exemplaire");
        removeExemplaireButton = new Button("Supprimer un exemplaire");

        manageBorrowingsButton = new Button("Gérer les emprunts");
        statisticsButton = new Button("Statistiques");

        setLayout(new BorderLayout());
        add(welcomeLabel, BorderLayout.NORTH);

        JPanel buttonsPanel = new JPanel(new GridLayout(4, 2, 10, 10));
        buttonsPanel.add(addBookButton);
        buttonsPanel.add(removeBookButton);
        buttonsPanel.add(editBookButton);
        buttonsPanel.add(addUserButton);
        buttonsPanel.add(removeUserButton);
        buttonsPanel.add(editUserButton);
        buttonsPanel.add(addExemplaireButton);
        buttonsPanel.add(editExemplaireButton);
        buttonsPanel.add(removeExemplaireButton);
        buttonsPanel.add(manageBorrowingsButton);
        buttonsPanel.add(statisticsButton);

        add(buttonsPanel, BorderLayout.CENTER);
    }

    public Button getAddBookButton() {
        return addBookButton;
    }

    public JButton getRemoveBookButton() {
        return removeBookButton;
    }

    public Button getEditBookButton() {
        return editBookButton;
    }

    public Button getAddUserButton() {
        return addUserButton;
    }

    public Button getRemoveUserButton() {
        return removeUserButton;
    }

    public Button getEditUserButton() {
        return editUserButton;
    }

    public Button getManageBorrowingsButton() {
        return manageBorrowingsButton;
    }

    public Button getStatisticsButton() {
        return statisticsButton;
    }

   }*/

    }
}